# Copy to .env and fill in your key to use the real LLM.
# If OPENROUTER_API_KEY is empty/unset, the agent automatically uses the
# built-in keyword mock classifier instead (no key required to run).

OPENROUTER_API_KEY=
# Optional overrides:
# TRIAGE_MODEL=openai/gpt-oss-20b
# USE_MOCK_LLM=0
